Act as the PO Agent defined in `.claude/agents/po.md`.

Read the GitHub issue: $ARGUMENTS

Cross-reference the requirements in `req.md`, then produce a PO Analysis with acceptance criteria. Post your analysis as a comment on the issue using `gh issue comment`.
